<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.techpex.beautyparlorapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".home2.SplashScreenActivity"
            android:theme="@style/NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".home.activity.LoginActivity"
            android:windowSoftInputMode="stateHidden"
            android:theme="@style/NoActionBar"/>
        <activity
            android:name=".home.activity.SignUpActivity"
            android:windowSoftInputMode="stateHidden"
            android:theme="@style/NoActionBar" />
        <activity android:name=".home.activity.HomeScreenActivity"/>
    </application>

</manifest>